server:
  port: 9090

spring:
  jpa:
    properties:
      hibernate.format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:ewm
    username: test
    password: test`

  mvc:
    format:
      date-time: yyyy-MM-dd HH:mm:ss
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml

app:
  default-app-name: ewm-main-service













